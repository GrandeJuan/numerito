export const AUTH = {
  SALT_ROUNDS: 10,
  MIN_PASSWORD_LENGTH: 8,
  TOKEN_EXPIRY_HOURS: 1,
};

export const TOTP = {
  TIME_STEP: 30,
  WINDOW: 1,
};

export const ACCOUNTING = {
  BALANCE_TOLERANCE: 0.01,
};
